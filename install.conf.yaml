# install.conf.yaml
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim
    ~/.config/alacritty: alacritty

- shell:
  - [git submodule update --init --recursive, Installing submodules]
